# SELinux Analysis Report

**Generated:** {{ ansible_date_time.iso8601 }}
**Host:** {{ vm_name }}
**SELinux Mode:** Permissive
**Policy:** {{ selinux_policy }}

---

## Summary

{{ selinux_analysis.summary }}

---

## Findings

{% for finding in selinux_analysis.findings %}
### {{ loop.index }}. {{ finding.denial }}

| Field | Value |
|-------|-------|
| Source Type | `{{ finding.source_type }}` |
| Target Type | `{{ finding.target_type }}` |
| Target Path | `{{ finding.target_path }}` |
| Permission | `{{ finding.permission }}` |
| Remediation Type | `{{ finding.remediation.type }}` |

**Recommendation:** {{ finding.recommendation }}

**Remediation command:**
{% if finding.remediation.type == 'fcontext' %}
```bash
semanage fcontext -a -t {{ finding.remediation.selinux_type }} '{{ finding.remediation.path_spec }}'
restorecon -Rv {{ finding.target_path | regex_replace('/[^/]*$', '') }}
```
{% elif finding.remediation.type == 'boolean' %}
```bash
setsebool -P {{ finding.remediation.boolean_name }} {{ finding.remediation.boolean_value }}
```
{% elif finding.remediation.type == 'port' %}
```bash
semanage port -a -t {{ finding.remediation.selinux_type }} -p {{ finding.remediation.protocol }} {{ finding.remediation.port }}
```
{% elif finding.remediation.type == 'module' %}
Custom policy module `{{ finding.remediation.module_name }}` required — see `{{ report_output_dir }}/{{ selinux_policy_module_name }}.te`.
{% endif %}

{% endfor %}

---

## audit2allow Assessment

{{ selinux_analysis.audit2allow_assessment }}

---

## Raw Data

- AVC denials: `{{ report_output_dir }}/avc-denials.log`
- audit2allow suggestions: `{{ report_output_dir }}/audit2allow-suggestions.txt`
- AI analysis: `{{ report_output_dir }}/ai-analysis.json`

---
*Report generated by automate-selinux pipeline*
